export class Iteacher{
    id:number=0
    firstname: string=''
    lastname: string=''
    mbnumber?: number
    classalorted!: number
    email!: string
    password:any
    address:any
    gender :string =''
  // ! ==> undefind(it means property not defind/exist in the class);
  // ? ==> null(it means property exist but value not set yet);
  // 0 ==> (defult value set to 0 for number deta types);
  // '' ==> (defult value set to emty string for string deta types);
}